# Python 参考实现（已降级，非产品主线）

历史 CLI + NiceGUI 实现。**产品方向是原生桌面（`mac/` → `windows/`）**。

本目录仅作：

- 行为对照 / 偶尔查阅
- 既有 pytest 用例

不再作为推荐安装与发布路径。日常开发请看仓库根 `README.md` 与文档站。

```bash
cd python
uv sync
uv run litnexus --help
uv run pytest
```
